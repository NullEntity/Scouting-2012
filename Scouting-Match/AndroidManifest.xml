<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nacteam93.scouting.match"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="13" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".MatchSelectActivity"
			android:label="@string/app_name">
			<intent-filter>
				<action android:name="matchAction"/>
				<category android:name="matchCategory"/>
			</intent-filter>
		</activity>
		<activity
		    android:name=".FieldActivity"
		    android:label="@string/app_name">
		    <intent-filter>
		        <action android:name="matchScoutAction" />
		        <category android:name="matchScoutCategory" />
		    </intent-filter>
		</activity>
    </application>

</manifest>